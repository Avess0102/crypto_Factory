/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "storage.tally.so",
      "assets.coingecko.com",
      "coin-images.coingecko.com",
    ], 
  },
};

export default nextConfig;
